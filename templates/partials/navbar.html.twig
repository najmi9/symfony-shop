<nav class="navbar navbar-expand navbar-light bg-light fixed-top">
    <a class="navbar-brand d-none d-md-block" href="{{ path('home') }}">We Shop Now</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item ">
                <a class="nav-link" data-bs-toggle="tooltip" data-bs-placement="top" title="To be shipped"
                    href="{{ path('orders_to_be_shipped') }}">To be shipped</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" data-bs-toggle="tooltip" data-bs-placement="top" title="Shipped"
                    href="{{ path('orders_shipped') }}">Shipped</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" data-bs-toggle="tooltip" data-bs-placement="top" title="Unpaid"
                    href="{{ path('orders_unpaid') }}">Unpaid</a>
            </li>

            <li class="nav-item">
                <a href="{{ path('cart') }}" class="nav-link" data-bs-toggle="tooltip" data-bs-placement="top"
                    title="Cart">
                    <i class="fas fa-cart-plus text-danger fa-1x"></i>
                    <span id="cart-products" class="text-success font-weight-bolder">
                        {{ (app.session.get('cart', [])|reduce((c, v) => c+v))|default(0) }}
                    </span>
                </a>
            </li>

            {% if app.user %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}" data-bs-toggle="tooltip" data-bs-placement="top"
                    title="Logout">logout</a>

                {% else %}
                <a class="nav-link " href="{{ path('app_login') }}" tabindex="-1" data-bs-toggle="tooltip"
                    data-bs-placement="top" title="Login">Login</a>
            </li>
            <li>
                <a class="nav-link" href="{{ path('app_register') }}" tabindex="-1" data-bs-toggle="tooltip"
                    data-bs-placement="top" title="Register">Register</a>
            </li>
            {% endif %}
        </ul>

        {% if is_granted('ROLE_ADMIN') %}
            <ul class="navbar-nav">
                <li class="nav-item ">
                    <a class="nav-link bg-success text-light rounded" data-bs-toggle="tooltip" data-bs-placement="top" title="Dashbord"
                        href="{{ path('admin_dashboard') }}">Dashbord</a>
                </li>
            </ul>
        {% endif %}
    </div>
</nav>